export default class Tarefa { 
    constructor(descricao , dataDeTermino, prioridade, status)
    {
        this.descricao = descricao;
        this.dataDeTermino = dataDeTermino;
        this.prioridade = prioridade ;
        this.status = status;
    }
 }